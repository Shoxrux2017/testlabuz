/// Identifies one authoritative parent Attempt publication, without encoding
/// its data. Child editor state records the token it synchronized from.
class StudentAttemptPublicationToken {
  StudentAttemptPublicationToken();
}
